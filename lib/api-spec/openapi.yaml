openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: Charles Mood Check-In API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: checkins
    description: Mood check-in operations
  - name: profiles
    description: Employee profile operations
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /checkins:
    get:
      operationId: listCheckins
      tags: [checkins]
      summary: List all check-ins (admin)
      parameters:
        - in: query
          name: date
          schema:
            type: string
          description: Filter by date (YYYY-MM-DD), defaults to today
        - in: query
          name: tzOffsetMinutes
          schema:
            type: integer
          description: Browser timezone offset in minutes (Date.getTimezoneOffset()) for correct local-day grouping
      responses:
        "200":
          description: List of check-ins
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CheckIn"
    post:
      operationId: createCheckin
      tags: [checkins]
      summary: Submit a mood check-in
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CheckInInput"
      responses:
        "200":
          description: Check-in created (or already checked in)
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/CheckIn"
                  - $ref: "#/components/schemas/AlreadyCheckedIn"

  /checkins/summary:
    get:
      operationId: getCheckinSummary
      tags: [checkins]
      summary: Get today's mood summary for admin dashboard
      parameters:
        - in: query
          name: date
          schema:
            type: string
          description: Date to summarize (YYYY-MM-DD), defaults to today
        - in: query
          name: tzOffsetMinutes
          schema:
            type: integer
          description: Browser timezone offset in minutes (Date.getTimezoneOffset()) for correct local-day grouping
      responses:
        "200":
          description: Dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckInSummary"

  /checkins/employee/{employeeId}:
    get:
      operationId: getEmployeeHistory
      tags: [checkins]
      summary: Get full check-in history for a specific employee
      parameters:
        - in: path
          name: employeeId
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Employee check-in history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeHistory"

  /checkins/trends:
    get:
      operationId: getCheckinTrends
      tags: [checkins]
      summary: Get mood trends for the past 7 days
      parameters:
        - in: query
          name: tzOffsetMinutes
          schema:
            type: integer
          description: Browser timezone offset in minutes (Date.getTimezoneOffset()) for correct local-day grouping
      responses:
        "200":
          description: 7-day mood trends
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TrendDay"

  /profiles/{employeeId}:
    get:
      operationId: getEmployeeProfile
      tags: [profiles]
      summary: Get an employee profile
      parameters:
        - in: path
          name: employeeId
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Employee profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeProfile"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string

    put:
      operationId: upsertEmployeeProfile
      tags: [profiles]
      summary: Create or update an employee profile
      parameters:
        - in: path
          name: employeeId
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeProfileInput"
      responses:
        "200":
          description: Upserted profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeProfile"

components:
  schemas:
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string

    CheckInInput:
      type: object
      required: [employeeId, employeeName, mood]
      properties:
        employeeId:
          type: string
        employeeName:
          type: string
        department:
          type: ["string", "null"]
        mood:
          type: string
          enum: [great, good, okay, calm, stressed, exhausted]
        energyLevel:
          type: ["integer", "null"]
          minimum: 1
          maximum: 10
        focusLevel:
          type: ["integer", "null"]
          minimum: 1
          maximum: 10
        stressLevel:
          type: ["integer", "null"]
          minimum: 1
          maximum: 10
        tags:
          type: array
          items:
            type: string
        note:
          type: ["string", "null"]
        intent:
          type: string
          enum: [create, update]
          description: When set to update, replaces the employee's existing check-in for today (business timezone).

    CheckIn:
      type: object
      required: [id, employeeId, employeeName, mood, checkedInAt]
      properties:
        id:
          type: integer
        employeeId:
          type: string
        employeeName:
          type: string
        department:
          type: ["string", "null"]
        mood:
          type: string
          enum: [great, good, okay, calm, stressed, exhausted]
        energyLevel:
          type: ["integer", "null"]
        focusLevel:
          type: ["integer", "null"]
        stressLevel:
          type: ["integer", "null"]
        tags:
          type: array
          items:
            type: string
        note:
          type: ["string", "null"]
        checkedInAt:
          type: string
          format: date-time

    AlreadyCheckedIn:
      type: object
      required: [alreadyCheckedIn, employeeName, checkedInAt, mood]
      properties:
        alreadyCheckedIn:
          type: boolean
          enum: [true]
        employeeName:
          type: string
        checkedInAt:
          type: string
          format: date-time
        mood:
          type: string
          enum: [great, good, okay, calm, stressed, exhausted]

    DepartmentStat:
      type: object
      required: [department, count, averageWellness, dominantMood]
      properties:
        department:
          type: string
        count:
          type: integer
        averageWellness:
          type: number
        dominantMood:
          type: string

    CheckInSummary:
      type: object
      required: [totalCheckins, moodBreakdown, averageEnergy, averageFocus, averageStress, teamWellnessScore, burnoutRiskCount, departmentBreakdown]
      properties:
        totalCheckins:
          type: integer
        moodBreakdown:
          type: object
          additionalProperties:
            type: integer
        averageEnergy:
          type: number
        averageFocus:
          type: number
        averageStress:
          type: number
        teamWellnessScore:
          type: number
        burnoutRiskCount:
          type: integer
        departmentBreakdown:
          type: array
          items:
            $ref: "#/components/schemas/DepartmentStat"
        topPositiveEmployees:
          type: array
          items:
            $ref: "#/components/schemas/CheckIn"
        needsSupportEmployees:
          type: array
          items:
            $ref: "#/components/schemas/CheckIn"

    EmployeeHistory:
      type: object
      required: [employeeId, employeeName, checkins]
      properties:
        employeeId:
          type: string
        employeeName:
          type: string
        department:
          type: ["string", "null"]
        checkins:
          type: array
          items:
            $ref: "#/components/schemas/CheckIn"
        averageEnergy:
          type: number
        averageFocus:
          type: number
        averageStress:
          type: number
        averageWellness:
          type: number
        burnoutDays:
          type: integer

    EmployeeProfileInput:
      type: object
      required: [fullName]
      properties:
        fullName:
          type: string
        department:
          type: ["string", "null"]
        email:
          type: ["string", "null"]
        phone:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        profileImageUrl:
          type: ["string", "null"]

    EmployeeProfile:
      type: object
      required: [id, employeeId, fullName, createdAt, updatedAt]
      properties:
        id:
          type: integer
        employeeId:
          type: string
        fullName:
          type: string
        department:
          type: ["string", "null"]
        email:
          type: ["string", "null"]
        phone:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        profileImageUrl:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        dominantMood:
          type: string

    TrendDay:
      type: object
      required: [date, totalCheckins, dominantMood, averageWellness]
      properties:
        date:
          type: string
        totalCheckins:
          type: integer
        dominantMood:
          type: string
        averageWellness:
          type: number
